elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.ousaimi.com"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.ousaimi.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.ousaimi.com
      server_name: ousaimi.com

dendrite:
  dendrite_config:
    global:
      server_name: matrix.ousaimi.com
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.ousaimi.com
    tls:
      - hosts:
          - matrix.ousaimi.com
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
